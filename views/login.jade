extends layout

block page
    - var nemu = 'login'

block content
    .col-md-4.col-md-offset-4
        h2 LOg in
        div= error
        div
            form(action="/login", method="POST")
                p
                    input.form-control(name="email", type="text", placeholder="hi@MC.co")
                p
                    input.form-control(name="password", type="password", placeholder="***")
                p
                    button.btn.btn-lg.btn-primary.btn-blcok(type="submit") Log in
